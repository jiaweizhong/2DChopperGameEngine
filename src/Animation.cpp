

#include "Animation.h"


Animation::Animation(int index, int numFrames, int speed):
                    index(index),numFrames(numFrames),animationSpeed(speed){
                        
                    };